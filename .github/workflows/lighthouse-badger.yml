name: "DISABLED: Lighthouse Badger"

on:
  workflow_dispatch: # Only manual trigger

# This workflow is disabled - requires LIGHTHOUSE_BADGER_TOKEN secret
# Re-enable after site goes live and secret is configured
